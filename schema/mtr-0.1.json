{
"$schema": "https://json-schema.org/draft/2020-12/schema",
"$id": "https://Tim-Heb.github.io/mtr/schema/mtr-0.1.json",
"title": "Minimum Truth Record (MTR)",
"description": "Schema for Minimum Truth Record v0.1 — Draft Stable",
"type": "object",
"required": [
"DecisionID",
"DecisionType",
"ModelID",
"PromptHash",
"Timestamp",
"Environment"
],
"properties": {
"DecisionID": {
"type": "string",
"description": "Unique identifier for this decision (UUID or ULID)"
},
"DecisionType": {
"type": "string",
"description": "Type of decision made (classification, recommendation, action, etc.)"
},
"ModelID": {
"type": "string",
"description": "Identifier of the model and version used"
},
"PromptHash": {
"type": "string",
"description": "Hash of the input prompt or structured input"
},
"ToolCalls": {
"type": "array",
"items": {
"type": "string"
},
"description": "Optional list of tools or APIs invoked"
},
"UncertaintySignal": {
"description": "Confidence, uncertainty, or risk indicator",
"oneOf": [
{ "type": "number" },
{ "type": "string" },
{ "type": "null" }
]
},
"HumanOverride": {
"type": "string",
"enum": ["none", "modified", "rejected"],
"description": "Whether a human modified or rejected the decision"
},
"Outcome": {
"type": ["string", "null"],
"description": "Observed downstream result"
},
"Timestamp": {
"type": "string",
"format": "date-time",
"description": "ISO 8601 timestamp of the decision"
},
"Environment": {
"type": "string",
"enum": ["production", "staging", "test"],
"description": "Execution environment"
}
},
"extensions": {
"type": "object",
"description": "Optional implementation-specific fields. Not required for core compliance.",
"additionalProperties": true
}
"additionalProperties": false
}
